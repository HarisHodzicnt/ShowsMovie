import { CssBaseline } from '@material-ui/core';
import { AppHeader } from '../../components/AppHeader';
import MainComponent from '../../components/MainComponent';

export const MovieShowsContainer = () => {
  return (
    <>
      <CssBaseline />
      <AppHeader />
      <MainComponent />
    </>
  );
};
